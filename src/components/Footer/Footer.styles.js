const wrapper = "w-full bg-white px-6 pb-12 pt-4 md:py-12";

const container =
  "max-w-7xl mx-auto flex flex-col lg:flex-row justify-between items-start gap-10 relative";

const logoWrapper = "lg:w-1/3";

const logoImg = "w-48 h-auto";

const description = "text-gray-800 text-base mb-6";

const form = "space-y-4";

const formRow = "flex flex-col md:flex-row gap-4";

const inputWrapper = "flex flex-col gap-4 w-full md:w-1/2";

const inputIcon = "absolute top-1/2 left-3 transform -translate-y-1/2 w-4 h-4";

const input =
  "w-full pl-10 py-2 border border-gray-400 rounded-full shadow-sm text-sm placeholder-gray-600";

const textareaWrapper = "w-full md:w-1/2";

const textarea =
  "w-full h-full min-h-[92px] p-4 border border-gray-400 rounded-2xl shadow-sm text-sm placeholder-gray-600 resize-none";

const submitSection = "flex flex-col sm:flex-row sm:items-center justify-between";

const submitText = "text-sm text-gray-700 mb-2 sm:mb-0";

const submitButtonBase =
  "w-full sm:w-auto px-6 py-2 rounded-xl font-semibold text-sm border-2 border-black transition duration-200";

const submitButtonActive = "bg-yellow-400 hover:bg-yellow-500 text-black";

const submitButtonLoading = "bg-yellow-300 text-black";

const copyright = "absolute -bottom-10 left-0 text-sm text-gray-600";

const scrollTopButton =
  'fixed bottom-5 right-5 z-10 cursor-pointer rounded-full bg-white/70 shadow-md transition-all duration-300 flex items-center justify-center w-10 h-10';

const scrollTopIcon = 'w-6 h-6 text-black';

const scrollTopAnimatiuon = "fixed bottom-5 right-5 z-10 cursor-pointer rounded-full bg-white/70 shadow-md transition-all duration-300 flex items-center justify-center w-10 h-10"

const styles = {
  wrapper,
  container,
  logoWrapper,
  logoImg,
  description,
  form,
  formRow,
  inputWrapper,
  inputIcon,
  input,
  textareaWrapper,
  textarea,
  submitSection,
  submitText,
  submitButtonBase,
  submitButtonActive,
  submitButtonLoading,
  copyright,
  scrollTopButton, 
  scrollTopIcon,
  scrollTopAnimatiuon
};

export default styles;